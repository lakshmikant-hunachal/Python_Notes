#input function:
# The input() function allows the user to provide input to the program. It reads a line from the input (usually from the user), converts it into a string, and returns it.
name = input("Enter your name: ")
print("Hello, " + name + "!")